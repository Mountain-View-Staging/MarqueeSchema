{
  "$comment": "SOURCE OF TRUTH for the Marquee project schema. Both MarqueeDataKit (Swift/GRDB) and MarqueeStudioWeb (JS/sql.js) generate from this file. Never hand-edit generated output in dist/. RESET 2026-07-23: the former v1..v14 append-only history was flattened into a single v1-baseline because nothing consumes these databases with a shipped MVP client yet. Until an MVP client exists, the baseline SQL may be edited in place. Once one ships, return to append-only — add v2-… etc., never edit the baseline — so deployed databases can still migrate.",
  "formatVersion": 1,
  "schemaName": "Marquee",
  "migrationTable": "grdb_migrations",
  "migrations": [
    {
      "identifier": "v1-baseline",
      "file": "sql/001-v1-baseline.sql",
      "note": "The full Marquee project schema as a single baseline, flattened from the former v1..v14 append-only history (2026-07-23)."
    }
  ]
}
